# Project Journal Notes
## Journal Guidelines
1. When requested by the user (i.e. "Please add to the journal"), add a log above the previous one
2. Run `date` to get the date and time, use that as the headline (Convert date to format-> Day MM/DD/YYYY HH:MM TMZ)
3. Write a brief summary of what has been changed or updated since the last journal. No need to document every little nuance. Include major changes, and current challenges at a high level
4. Treat them like a substantially detailed commit message with some details but keep it brief, 3-5 sentences at most.


#### Entry: Sun 06/14/2026 11:37 PDT
Round 2 inline-editable refactor shipped (`netwerk-3-bug-fixes`). Foundational change: new `useInlineField` hook makes `session.dirtyFields` the single source of truth per field — `PostPageClient` and `EventPageClient` were rendering `displayContent` from raw server values, so typed text appeared to vanish on blur even though the session held it correctly; that's fixed. All parallel `editX` local state and `cancelRevision` reset boilerplate removed from all three consumers (Post, Event, Profile). `InlineDateTimePicker` folded into the batch (no longer saves immediately on its own). `CoverImageEditor` now defers the upload — file is held in session, upload runs at commit time, Cancel discards it cleanly. `InlineEditSession` extended with `commit({ publish })`, `dirtyFiles`/`setDirtyFile`/`onCommitFiles`, and `publishable`/`canPublish`/`publishHint` props; the save bar now merges Save + Publish into one place (Save / Save-and-publish / Publish depending on draft state and pending changes). Profile stuck-in-edit bug fixed (`setPreviewMode(true)` on save). Published-post-still-editable fixed (drops out of edit mode on PUBLISHED transition). Two Playwright tests updated for `exact: true` on the "Save" button locator (bar now also has "Save and publish").

#### Entry: Sun 06/14/2026 10:48 PDT
Round 1 bug fixes shipped (`netwerk-3-bug-fixes`), 6 tickets moved to QA. Event authorship PATCH route now persists `pageId`; page avatar upload routes to the correct page endpoint instead of always hitting the user; event cover uploads now replace the banner (with owner delete button added to `ImageCarousel`); event date year field capped at 4 digits; profile search changed from `startsWith` to `contains`; session→login routing hardened with cookie prefix-match and prefetch-safe redirect gate. Three new feedback-form bugs filed to BUGS epic — two inline-editable ones deferred to round 2.

#### Entry: Sun 06/14/2026 10:21 PDT
Tooling fix (no code change): documented the reliable way to pull Notion tickets. While pulling the BUGS epic for `netwerk-3-bug-fixes`, semantic `notion-search` returned only 9 of 19 tickets and I initially presented that as complete — the hosted Notion MCP connector has no row-query tool and its search is relevance-ranked/incomplete with no error to signal it. The correct path is a direct REST query: `POST /v1/databases/{id}/query` with `NOTION_KEY` + `NOTION_TICKETS_DB` from `.env.development` and a property filter — that returned all 19 (7 open, 12 done). Root cause of the miss: a thorough how-to already existed at `docs/PULL_TICKETS.md` but nothing in the bootstrap linked to it, so the agent never saw it and re-derived the method the hard way. Fix: added a "Pulling tickets" pointer to that doc in `.claude/CLAUDE.md` (so it's surfaced at session start), corrected `PULL_TICKETS.md`'s Epic enum (added `🥩 MEATUP`, `⌘ NETWERK`), and reinforced the `feedback_notion_completeness` memory.

#### Entry: Mon 06/08/2026 13:00 PDT
Transactional email foundation shipped (Netwerk, `netwerk-2` branch). Stood up a swappable Resend sender service (`src/lib/email/`) with React Email templates (Layout, VerifyEmail, PasswordReset) in the project palette; `sendEmail()` is the single provider choke point with try/catch logging and a dev console fallback (no API key needed locally). Added `User.emailVerified`, `EmailVerificationToken`, and `PasswordResetToken` schema models (cloned from `SignupInvite`); migration backfills all existing users as verified so the new login gate locks no one out. Account verification is now enforced at login — invite-based signups receive an email, dev-bypass accounts are auto-verified, and a resend endpoint + UI affordance is available on the login and check-inbox pages. Password reset flow also complete (`/forgot-password`, `/reset-password`, always-200 no-enumeration endpoint). 18 new unit/route tests + 3 new E2E tests (login gate blocked, password reset loop, forgot-password neutrality); 173 unit + 46 E2E all green. `RESEND_API_KEY` added to `.env.development` and Vercel; `EMAIL_FROM` defaults to sandbox sender until the domain-verification ticket is actioned. Follow-up tickets filed for Activity Notifications dispatcher (Ticket B, NETWERK Backlog) and domain verification.

#### Entry: Fri 06/05/2026 12:12 PDT
Visibility model shipped (Netwerk P0). Renamed `PageVisibility` → unified `Visibility` enum; added `visibility` to User, Event, and Post (replacing `User.isPublic`); migrated and backfilled dev DB. Built centralized enforcement layer (`src/lib/utils/server/visibility.ts`) with viewer-aware list-mode filters and detail-mode gates — all existing scattered draft-gating sites consolidated here. Cascade-sync keeps `Post.visibility` in step when a parent User/Page/Event flips; Public→Private page flip converts existing followers to MEMBER permissions in the same transaction. `VisibilitySelector` UI (radio-group + compact select) wired into profile and page inline-edit sessions. Seed extended with `secret-workshop` (PRIVATE) and `unlisted-zine` (UNLISTED) fixtures. 30 new unit tests, 11 new E2E tests; 155 unit + 43 E2E all green. Follow-up ticket for Request-to-Follow/Request-to-Join approval flows filed in ProLib Tickets (NETWERK, P1) — prod migration still needs user approval before applying to Supabase.

#### Entry: Sat 05/09/2026 12:03 PDT
Prod sync and bug fixes. Applied the final missing migration (`add_event_timezone`) to prod Supabase — the other four post-signup-invites migrations had already been applied manually. Reseeded prod and uploaded missing example images to Supabase storage (4 avatar/event images plus `spats-2-anniversary.jpg`). Fixed e2e test failures: three tests still referenced the old `/profile` route and "Profile Settings" heading after the rename to `/settings`; updated to match current routes. Fixed a bug where page-owned events appeared on the creator's personal profile — `getEventsByUser` and `GET /api/events?userId=` were missing the `pageId: null` filter that `getPostsByUser` already had.

#### Entry: Sat 05/09/2026 10:52 PDT
M4 polish items (polish-2 branch). **Three new pages**: custom `not-found.tsx` (404 page with friendly copy, "Back to Explore" link, and a bug report link); `/about` rewritten with structured placeholder sections (What is PL?, What You Can Do, Our Values, Who's Behind This, Get Involved) using ash-green outline cards to signal draft content; new `/guidelines` route with placeholder community guideline sections (Respect & Kindness, Sharing & Authenticity, Privacy & Safety, Content & Communication, What Happens If Broken) and a cross-link back to About. `GUIDELINES` constant added to `routes.ts`; `"guidelines"` added to `RESERVED_HANDLES`. **Footer**: split the single "About & Community Guidelines" link into two separate links. **WelcomeBanner**: replaced inline SVG dismiss button with the existing `XCircleIcon`; added "About the Project Library" as the first action link. **NavProfileTag**: trigger and all switcher items now use the same `size="md"` `ProfileTag`; trigger wrapper got `px-3 py-1` padding to match the switcher item row padding — both the top card and the in-dropdown cards now render at the same width with the same avatar size.

#### Entry: Fri 05/08/2026 16:37 PDT
Event timezone support (polish-2 branch). Added nullable `eventTimezone String?` to the Event schema (migration `add-event-timezone`). Seed data now uses a human-readable split format — `eventDate`, `eventStartTime`, `eventTimezone` (e.g. `"PST"`) — instead of opaque ISO Z strings; the seed script resolves abbreviations to IANA identifiers and computes the correct UTC instant using `Intl.DateTimeFormat` offset math. `formatDateTime()` updated to render times in the event's local timezone and append the abbreviation ("Sep 1, 2026, 6:00 PM PDT"); API routes (POST/PATCH), types, validations, `event-client.ts`, and `eventBaseFields` all updated to pass `eventTimezone` through. `InlineDateTimePicker` gained a timezone dropdown (Pacific/Mountain/Central/Eastern/Alaska/Hawaii/UTC) that saves alongside the datetime.

#### Entry: Fri 05/08/2026 14:47 PDT
Settings/profile UX rework (polish-2 branch). **Route consolidation**: `/profile` deleted (no real users, no redirect needed); `/settings` now renders `ProfilePageView` directly; `PROFILE` route constant removed; `HamburgerMenu` and `proxy.ts` updated to use `SETTINGS`. `ActingAsPageTooltip` and `SettingsPageView` deleted as deprecated dead code. **Expanded personal info form**: `/settings/personal-info` rewritten using `InlineEditSession` + `InlineEditable` — works for both users and pages; user fields are firstName/middleName/lastName (private) and displayName/headline/bio/location/interests (public, with EyeIcon + "Public" indicator); page fields are name/headline/bio/location/interests (public) plus an optional address section; email shown readonly; `PUT /api/me/page` extended to accept `name`. Both settings views gained "Edit Personal Information" and renamed "Edit Public Profile" buttons. **Profile edit/readonly reversal**: new `OwnProfileView` client component manages the readonly↔edit toggle — own profile at `/[handle]` defaults to readonly public view with a pencil "Edit" button; clicking Edit enters `ProfileEditClient` which shows a "Preview" button (`onPreview` callback prop added) to return to readonly. `?edit=true` query param supported for deep-linking from the "Edit Public Profile" settings button. **Pages list in settings**: `PageSwitcher` simplified — "View Page" button removed, whole card wrapped in a `Link` (with `asLink={false}` on `ProfileTag` to avoid nested anchors).

#### Entry: Fri 05/08/2026 13:46 PDT
Beta UX layer (polish-2 branch). **Global profile search**: new `/search` page (`CenteredLayout` + `ProfileSearchPage` client component) with debounced search, responsive masonry columns (reusing `useColumnCount`), and result cards showing avatar, name, handle, headline, and interest tags. Single combined API at `GET /api/search/profiles?q=&type=user|page|all` backed by a `searchProfiles` server utility; `ProfileSearchDropdown` migrated to `?type=user` as a composed subset; old `/api/users/search` route deleted. "Profile search" entry added to the hamburger menu with magnifying glass icon; `SEARCH` route constant and reserved handle added. **Landing page headers**: short warm text above each of the four landing images. **Welcome banner**: dismissible inline card on `/explore` (localStorage-gated) with action links (set up profile, find people, post something) and early-beta note; removed the self-referential "Browse" link that pointed at the page the banner was already on. **Beta messaging**: `InviteCTA` updated to "early beta" copy with earthy border/bg; beta note added to site footer. **Empty draft cleanup**: `EventPageClient` and `PostPageClient` now only auto-delete a draft on navigate-away if it has no content — `hasContentRef` tracks title/content/location/tags and dirty inline fields, so drafts with any user input are preserved. Event delete redirect fixed to `/explore`; E2E tests updated throughout.

#### Entry: Fri 05/08/2026 11:12 PDT
Four M3 polish items landed plus two cross-cutting unifications. **DeleteConfirmButton** (new shared component) replaced the near-identical `DeletePostButton` and `DeleteEventButton`, and is also used for about page content deletion — same confirmation UX, `destructive` / `subtle` variants. **About Page delete**: `AboutPageClient` now shows a Delete button when content exists; on confirm it calls the API to null `aboutContent` and redirects to the profile via `router.push(PUBLIC_PROFILE(handle))` (a bug found during testing — the initial version left the user on the now-empty about URL; also fixed a pre-existing monospace font on the textarea). **Publish validation hints**: post and event draft publish buttons now show a small text hint below when disabled due to missing content/title. **Image auto-compression**: `compressImage()` added to `image.ts` (Canvas API, iterative JPEG quality reduction); `useImageUpload` hook auto-compresses files exceeding 5 MB, exposes `wasCompressed`, and both `CoverImageEditor` and `AvatarEditModal` show an amber informational notice when compression occurred. **Edit Personal Info**: new `/settings/personal-info` route with a form to edit private `firstName`/`middleName`/`lastName` fields; PUT route extended to accept these fields; Settings page links to it; page identity guard shows "Coming soon."

#### Entry: Fri 05/08/2026 10:28 PDT
Three Bundle A polish items landed. **URL param persistence (P0)**: `useFilterParams` was extended to be bidirectional — it now returns an `updateUrl(state)` function that builds a minimal query string (omitting defaults) and calls `router.replace()`, keeping the existing `useFilterParams` → `useFilter` architecture intact and making the pattern reusable for any collection view page. The explore page gained two sync effects: an immediate one for type/sort/view/tags changes, and a debounced one (piggybacks on the existing 300ms search debounce) for search. A `getPersistedFilterUrl` helper saves the current explore URL to `sessionStorage` so the "← Back to Explore" breadcrumbs on `PostPageClient` and `EventPageClient` restore the user's last filter state. **Pin icon hover (P1)**: `CollectionCard` container got a Tailwind `group` class; the pin button is now `opacity-0 group-hover:opacity-100` by default and `opacity-100` when already pinned — purely a CSS change. **Profile empty states (P1)**: `EmptyState.getItemLabel()` now handles `"post"` → `"posts"` and `"all"` → `"posts & events"`, so filtered-empty states on profile pages say "No posts yet." or "No events yet." instead of the generic "No items yet." All 125 unit tests, 32 E2E tests, typecheck, lint, and build pass clean.

#### Entry: Thu 05/07/2026 21:19 PDT
Fixed broken Leaflet map tiles — root cause was a CSS race condition in `useLeaflet.ts` where map init could fire before the stylesheet loaded; rewrote the hook as a module-level singleton that waits for both CSS and JS before resolving. Extracted a shared `LeafletMap.tsx` base component (handles wrapper, init, tile layer, cleanup, loading/error states) so `InteractiveMap` and `CollectionMap` are now thin wrappers adding only their specific behavior (draggable marker vs. multi-marker + radius circle). Created a shared `LocationSearchInput.tsx` (debounced Nominatim autocomplete dropdown with `onMouseDown`/`preventDefault` to survive input blur) used by both the event location field and the collection map controls — eliminated the redundant geocoding round-trip in `MapControls`. Fixed location not persisting on blur by displaying pending edit values (`editLocation ?? event.location`), removed the lat/lng coordinates display from the event edit page, and added auto-geocoding to `prisma/seed.ts` so new seed events only need a location string. Test handle fixtures updated (`alice.example`/`sam.example`).

#### Entry: Thu 05/07/2026 17:47 PDT
Map view polished as part of M2 UI cleanup. Added a controls bar above the map (`MapControls.tsx`) with a location text input (geocodes via Nominatim/OpenStreetMap, no API key) and radius pill buttons (5/10/25/50 mi, default 10), styled to match the existing FilterBoard. Client-side Haversine filtering in `FilteredCollection` hides markers outside the selected radius, with a melon-green `L.Circle` overlay on the map as a visual affordance. Footer count is now viewport-aware — updated via Leaflet's `moveend` event — instead of the previous static total. Map sizing changed from `h-96` to a `paddingBottom: 'min(75%, 70vh)'` technique: the `aspect-ratio + max-height` approach didn't work on iOS Safari because WebKit doesn't propagate computed height from that combination to absolutely-positioned children (Leaflet reads `clientHeight=0` and renders nothing); the padding-bottom trick gives the wrapper a concrete computed height before Leaflet initializes. Design system palette fixes throughout (removed generic `gray-*` classes in favor of `soft-grey`, `ash-green`, etc.).

#### Entry: Thu 05/07/2026 13:53 PDT
Reworked explore page sorting so events no longer dominate the top of the feed. The "All" tab now sorts everything by `createdAt`, so events intermix with posts by post date. The "Events" tab sorts by `eventDateTime` with upcoming events first (soonest → furthest) and past events after (most recently passed → oldest). Past events are visually greyed out (`opacity-50`) with a "Past" badge on the card.

#### Entry: Thu 05/07/2026 13:15 PDT
Rewrote the seed system and updated Playwright e2e tests. Seed data is now organized as self-contained per-user and per-page JSON packets in `prisma/seed-data/users/` and `prisma/seed-data/pages/` (alice, sam, portland-makers-guild) with cross-user relationships in a single `relationships.json`; the old flat `users.json`, `events.json`, `projects.json`, and `images.json` are deleted. `prisma/seed.ts` was rewritten (~400 lines, down from 707) to scan the packet directories, supports a `$env:VAR_NAME` password pattern for real user profiles, and exports `SeedUserPacket`/`SeedPagePacket`/`SeedRelationships` types. Added `user.template.json` and `page.template.json` as copy-paste starting points for new packets. Playwright config gained a `globalSetup` that checks whether the dev DB is seeded and runs `db:seed:dev` automatically if not — tests are now self-sufficient on a fresh checkout. All 6 e2e test files updated to use alice/sam (removed dolores/george references); all 32 tests pass.

#### Entry: Thu 05/07/2026 11:45 PDT
Connections view reworked: replaced redundant "View" buttons with expandable inline action icons (ellipsis → action button → X to close) across all tabs — Remove Follower, Unfollow, Leave Group, Remove from group (with inline last-admin error display), and an "Add members" entry point for page admins using `ProfileSearchDropdown`. Extended `DELETE /api/follows/[id]` with a `?removeFollower=` param for the reverse-direction delete. Fixed several issues found during testing: page admin detection derived from membership data instead of missing `entity.role`, ellipsis added to PAGE items in follower/following lists, `ProfileSearchDropdown` dropdown clipping fixed by removing `overflow-hidden` from `TabbedPanel`. Separately: fixed post avatar disappearing on publish (added `avatarImage` nested select to `postFields` in the PATCH route), fixed a pre-existing e2e test by adding a missing Dolores–Alice DM to seed data, and implemented edit mode gating on both `PostPageClient` and `EventPageClient` — published posts/events now show read-only by default with an Edit/Done toggle in the footer for owners. Added "← Back to Explore" breadcrumb above the card (via new `breadcrumb` prop on `PostPageShell`) and top-aligned the card layout (`items-start` instead of `items-center`).

#### Entry: Wed 05/06/2026 20:05 PDT
Root cause of page showing zero posts: all seeded posts defaulted to DRAFT (Prisma `@default(DRAFT)`) because the seed never set `status: "PUBLISHED"` on page posts or user posts from projects — fixed in `prisma/seed.ts`; dev DB was reset and reseeded. As part of the same branch, `PostPageClient` was brought to parity with `EventPageClient`: added `DropdownProfileSelector` for switching publish identity (draft-only, read-only once published), `ShareButton`, a Message button for non-owners, `handleAuthError` extraction, and a Home footer link; `posts/new` was converted to a client component that calls `createDraftPost` (matching `events/new`); `PATCH /api/posts/[id]` now accepts `pageId` to support author switching; `toPostCollectionItem` was fixed to include the missing `status` field. Removed two debug `console.log` statements left from the filtering work. Lint cleanup: ESLint config now suppresses `no-console` for all `src/app/api/**`, `src/lib/utils/server/**`, `scripts/**`, and `tests/**` paths via an override block (eliminating ~90 warnings), adds `varsIgnorePattern: "^_"` to the unused-vars rule, and fixes five structural issues (unused `updatedPage` var, `resource` → `_resource` in `InlineEditSession`, stale disable directives in `MessagesPageView` and `ActiveProfileContext`, removed `browser` from a test signature). E2E test fixes: updated `waitForURL` regex in `authoring.spec.ts` to exclude `/posts/new` via negative lookahead, and updated three messaging tests from "Dolores Example" to "George Example" to match the seeded alice↔george DM. 

#### Entry: Wed 05/06/2026 18:59 PDT
ProfileSearchDropdown added for the Manage Admins flow. Replaced the raw text handle input (`AddConnectionSearch`) with a debounced autocomplete dropdown (`ProfileSearchDropdown`) that fetches from a new `GET /api/users/search?q=` endpoint and renders avatar + display name + handle rows. Wired into `ManageConnections` with `excludeUserIds` to filter already-added members. Fixed a display name bug at the root: `createUser()` now computes `displayName` from `firstName + lastName` if not explicitly provided, and seed data was updated to set `displayName` from the `name` field — fixes all connection/profile surfaces showing handles instead of names. Two small UX tweaks: admins list now renders above the search form so opening search doesn't push the list down, and the Add button toggles to "Cancel" when the search is open.

#### Entry: Wed 05/06/2026 13:12 PDT
Four microsite UI improvements (Milestone 2 polish). **About Page entry point**: the `+ New element` dropdown (`AddElementButton`) now includes an "About Page" option (with separator) that navigates to `/{handle}/about` for inline WYSIWYG editing; the option is hidden once about content already exists. **Settings header avatar**: `ProfileSettingsBase` now accepts an `avatarEntity` and renders a `ProfilePicture` as `titleIcon` in `SettingsSection`, so both "User Settings" and "Page Settings" headers show the active profile's avatar. **NavProfileTag sizing**: trigger is now fixed at `w-[200px]` on desktop (names truncate — `truncate` added to compact-variant `ProfileTag`) and collapses to avatar-only below `sm` breakpoint via responsive show/hide wrappers. **Photo caption editing**: `ImageCarousel` now accepts `isOwner` — owners see an inline "+ Add caption" / "Edit caption" affordance below the image that PATCHes `/api/images/:id` and updates local state on save. `npm run validate` clean: 0 type errors, 125 unit tests, 32 E2E tests, build succeeds.

#### Entry: Wed 05/06/2026 12:51 PDT
RSVP form improvements: autofill + duplicate prevention for logged-in users. The event detail server page now fetches the logged-in user's name/email and checks for an existing RSVP on page load, passing all three down as optional props through `EventPageClient` to `RsvpForm`. `RsvpForm` initializes its state from those props — pre-filling name and email, and opening directly in the confirmation state if an RSVP already exists. Added `getRsvpByEmail` to `src/lib/utils/server/rsvp.ts`. Non-logged-in users are unaffected (blank form as before). `npm run validate` clean.

#### Entry: Wed 05/06/2026 12:24 PDT
N+1 refactor sweep and dead code removal. Three API routes that bypassed the server utility layer were fixed: `pages/[pageId]/events` and `pages/[pageId]/posts` now delegate to `getEventsByPage()` and `getPostsByPage()` respectively (adding batch image loading, `_count`, and `recentUpdate` to both); `posts/route.ts` dropped a local `postFields` duplicate in favor of the shared `postWithUserFields` from `fields.ts`. Dead exports removed: `getAllEvents` from `event.ts`; `getPostsForPage`, `getStandalonePosts`, and `getPostById` from `post.ts`; deprecated `EventCard.tsx` deleted. `getPostsForEvent` renamed to `getEventUpdates` and `getPostUpdates` retained — both now use `postWithUserFields` and the private `postSelectFields` local is gone. A ticket was drafted for a follow-on fix: `FollowStats` fetches full follower/following arrays just for `.length`; fix is to add `_count` to `publicUserFields`/`publicPageFields` and pass counts down from the profile server component as props. Three bugs fixed: `GET /api/posts` now accepts a `search` param and applies a case-insensitive title/content filter (matching events); seed updated to create the alice–george DM (was alice–dolores, which left a stale empty thread in dev); PATCH publish path now validates that the post has non-empty content server-side before flipping status to PUBLISHED.

#### Entry: Wed 05/06/2026 10:36 PDT
Component organization cleanup. `profile-element/` dissolved — `ProfileElementList`, `ProfileElementCard`, and `AddElementButton` moved to `profile/`; `LinkEditor` and `TextEditor` moved to `inline-editable/editors/`. `ProfileElementCard` refactored into a pure display component, with `ProfileElementList` now wrapping elements in `InlineEditable` so all inline editing shares one base pattern. `TabbedPanel` moved to `layout/`. Barrel exports removed from `connections/`, `profile-settings/`, and the dead `cards/` directory (deleted entirely). `ImageUploadModal` and `FollowButton` in `ui/` were superseded stubs with no consumers — deleted. Bug fix: `getUserDisplayName` now falls back through `firstName`/`lastName` before handle, fixing profile headers and message inboxes showing lowercase handles instead of display names.

#### Entry: Tue 05/05/2026 18:36 PDT
Profile UX overhaul (PR3 follow-up): consolidated `UserProfileClient` and `PageProfileClient` into a single `ProfileEditClient` component accepting a `{ type, data }` discriminated union and a `saveUrl` prop. Headline and location moved into the profile header (displayed italic for headline); bio italic removed; Tags, Address, and `isOpenToCollaborators` removed from display and fully purged from schema, types, API routes, and validations. `ProfileElementList` now renders in the owner view with WYSIWYG inline editors — no card borders in read mode, click to edit in place, trash icon only visible when editing, no Discard button (changes persist in session as you type). `ProfileElementKind` enum replaced with `LINK + TEXT` only (dropped `SOCIAL_LINK` and `CTA`); `LinkEditor` added with platform auto-detection (Instagram, GitHub, Twitter/X, etc.); `AddElementButton` rewritten using the existing `DropdownMenu` component. Added `EyeIcon` and `TrashIcon` to the shared icons file. Preview toggle button added (last in the button column) — sets `canEdit=false` on the session so all edit affordances auto-hide. Seed data updated with new element kinds; dev DB re-migrated and re-seeded.

#### Entry: Tue 05/05/2026 10:17 PDT
Completed the session-scoped route refactor as a follow-up to PR2. `/profile`, `/settings`, and `/connections` are now top-level routes (joining `/messages`) that resolve the active profile entirely from the session — no handle in the URL. The old handle-keyed manage routes (`/[handle]/profile`, `/[handle]/profile/settings`, `/[handle]/connections`) were deleted entirely (404). `UserSettingsContent` and `PageSettingsContent` were adapted in place; three new thin client wrappers were added (`ProfilePageView`, `SettingsPageView`, `ConnectionsPageClient`) that read `useActiveProfile()` from context and delegate to the appropriate existing component. `HamburgerMenu` was simplified to a static `PROFILE` link (no more async handle fetch), `ActingAsPageTooltip` was marked `@deprecated`, `proxy.ts` was updated to gate the three new routes behind login redirects, and `"connections"` was added to `RESERVED_HANDLES`.

#### Entry: Sun 04/26/2026 11:33 PDT
URL Flattening PR (PR 2) — all 17 tasks complete. Tasks 11–17 finished: route constants in `lib/const/routes.ts` replaced (all `PRIVATE_USER_PAGE`, `PUBLIC_PAGE`, `PAGE_PROFILE_SETTINGS`, etc. removed; unified `PUBLIC_PROFILE`, `MANAGE_PROFILE`, `MANAGE_CONNECTIONS`, etc. added); 157 TypeScript errors swept across type definitions, server utilities, API routes, and ~30 components; old route trees `src/app/u/` and `src/app/p/` deleted (clearing the `.next` cache was required to resolve stale validator references); Playwright tests updated throughout (`/u/alice` → `/alice`, `/p/portland-makers-guild` → `/portland-makers-guild`, `slug` → `handle` in all fixtures, auth test updated to expect `notFound()` instead of login redirect for ungated routes); Vitest unit tests updated (`username`/`slug` fixtures → `handle`); `ci-check-reserved-handles.ts` updated to skip dynamic `[handle]` segments; all validate gates pass: `tsc --noEmit` clean, `eslint` 0 errors, 125 unit tests pass, 10 reserved-handle segments verified.

#### Entry: Sat 04/25/2026 11:12 PDT
URL Flattening PR (PR 2 of the three-PR sequence) — Tasks 1–10 of the implementation plan complete; Stages 4–5 (cutover sweep + verification) still ahead. Schema introduces a single `Handle` table as the source of truth for unique handles across User and Page (each linked via mutually-exclusive `@unique` FKs and a `handleRecord` back-relation), with `User.username` → `User.handle` and `Page.slug` → `Page.handle` field renames; seed and Stage 2 routes (signup, page creation, invite consumption) write Handle rows transactionally via Prisma nested writes, and `/api/users/by-username/[username]` was renamed to `/api/users/by-handle/[handle]`. Stage 3 built the unified `/[handle]/...` route tree: `[handle]/page.tsx` dispatcher (`findEntityByHandle` → `getUserByHandle`/`getPageByHandle` per branch, `notFound()` otherwise), manager-gated `[handle]/connections/page.tsx` collapsing four legacy connection routes into one (intentional behavior change: previously-public connection lists are now `canManageEntity`-gated), and `[handle]/profile/{,settings}` with two sibling settings components (`UserSettingsContent` + `PageSettingsContent`) over a shared `ProfileSettingsBase`. Carry-over breakage: helper Prisma `select` clauses (`eventCollectionFields`, `getPagesForUser`, etc.) still reference removed `slug`/`username` columns and 500 at runtime on the events/posts fetch path — same failure on both new and legacy routes, confirming the dispatcher is structurally correct; the cleanup plus `PublicUser`/`PublicPage` type renames, `.username`/`.slug` reads, and route-constant renames all land together in Task 12 (sweep). Tagged the bridging `username:`/`slug:` field-name aliases I had to leave at component call-sites with a `PR2-TASK12-ALIAS` grep marker so the sweep can't miss them, and compressed ~145 lines of chronological session-journal at the bottom of the plan doc into ~60 lines of topical operational notes (env gotchas, design decisions, plan deviations, end-of-Stage-3 known breakage).

#### Entry: Sun 04/19/2026 11:58 PDT
* The production site was down for some reason, I debugged and realized that I had pushed updates to main that had a schema migration, and hadn't thought to apply to migrations to prod and so the site has probably been down for more than a week without me realizing it. I'll need make a plan to set up basic uptime and anomaly reporting for 400s/500s.
Authoring unification PR (#17, spats-4) written and opened for review — prerequisite for the Page Microsite work. Core addition is `InlineEditSession`: a context provider that batches dirty fields across a whole resource and saves them in one PATCH, replacing the old per-field Save/Cancel pattern everywhere. `PostStatus` enum (DRAFT/PUBLISHED) added to schema with migration and backfill; `/posts/new` and `/pages/new` now create a minimal server-side record and redirect immediately to the detail/profile page as the sole editing surface; dedicated `/p/profile/edit` and `/u/profile/edit` routes deleted. New client components: `PostPageClient`, `PageProfileClient`, `UserProfileClient`. Planning docs for the three-PR sequence (PR1 authoring → PR2 URL flattening → PR3 Microsite) written to `docs/scratch/`. Code review of the PR flagged four issues to address before merge: stale closure that deletes a published post on navigate-away, missing empty-content guard on the Publish button, `PostStatus` typed as a hand-written string union instead of the Prisma enum, and a draft-visibility gap in `GET /api/posts` when called with only `pageId`.

#### Entry: Sat 04/11/2026 15:29 PDT
Stretch of polish and restructuring between the Spats-2 and Spats-3 PR merges. **Signup/login polish** (04-04): added signup warnings, expanded interest form, social link field, and exposed the invite CTA on the login page as well. **Inline editing rework** (04-05): new `InlineEditable`/`InlinePlaceholder`/`TagInputField` primitives and the "inline tags picker" — `EventPageClient` and `FilterBoard` both slimmed significantly as editing moved inline. **Post page redo** (04-08): restructured `posts/[id]` and `posts/new` around new `PostPageShell`/`PostContentArea` layout components; also added `NavProfileShell` and `ImageUploadModal`. **Docs pass** (04-11): STATUS.md rewritten as a four-milestone tracker gated on closed beta (not "Spats = beta"), CLAUDE.md updated with the session-start bootstrap (read STATUS/JOURNAL/guidelines/memory in parallel), and a planning discussion confirmed the rest of the work should be split — microsite schema (M2 T2) and M3 design-polish list are the real unknowns, while bugs are already triaged and ranked in the Notion BUGS epic.

#### Entry: Fri 04/03/2026 22:58 PDT
Gated new account creation behind email invites: added `SignupInvite` Prisma model (hashed token, expiry, single-use), signup UI reads `?invite=` only (no invite-code field), and `POST /api/auth/signup` consumes the invite in a transaction. Added `DEV_SIGNUP_BYPASS_SECRET` for local `next dev` and Playwright when set (≥20 chars, constant-time compare; disabled when `NODE_ENV=production`), plus `npm run invite:create` / `scripts/create-signup-invite.ts` and `scripts/create-signup-invite.md`. Declared Prisma/pg as `serverExternalPackages` in `next.config.ts` to avoid bundling issues with interactive transactions. Tests: `tests/env.ts` loads dotenv for DB helpers; `public.spec.ts` covers signup with vs without invite; `auth.spec` uses bypass when the secret is present else creates a DB invite.

#### Entry: Fri 04/03/2026 18:00 PDT
Implemented Tasks 3–6 from the SPATS_LAUNCH milestone. Task 3 (Pinned Posts): added `pinnedAt` to Post and Event models, pin icon on CollectionCards for all collection item types, max-3 enforced in both APIs, pinned items float to top on profile/page views only. Task 4 (Profile Pictures): brown ring on profile avatar, clickable on own profile opening an upload/remove modal using the existing AboutModal pattern, session detection on `/u/[username]` via `auth()`. Task 5 (Image Captions): `caption` field added to Image model, new `PATCH /api/images/[id]` endpoint, caption banner on post/event detail pages via `showCaptions` prop on `ImageCarousel`. Task 6 (Map View): extracted shared `useLeaflet` hook from `InteractiveMap`, new read-only `CollectionMap` component with multi-marker Leaflet map and popup links, replaces the "coming soon" placeholder in `FilteredCollection`. Also added local filesystem fallback for image uploads in dev — when `NEXT_PUBLIC_SUPABASE_URL` is not set, files write to `public/uploads/` and are served as static assets, so uploads work locally without Supabase credentials.

#### Entry: Thu 04/02/2026 15:40 PDT
Aligned User and Page public profile pages onto shared components: `ProfileHeader` (avatar + name + @handle), `ProfileButtons` (Follow + Message as `TransparentCTAButton`s, disabled on own profile), and `ProfileBody` (FollowStats, shared fields, Page-specific address/tags/collaborator badge). Both `/u/[username]` and `/p/[slug]` now compose the same three components via the existing `ProfileEntity` discriminated union. Deleted `UserProfileHeader`, `UserProfileDisplay`, `ProfileOptionsMenu`, `PageProfileOptionsMenu`, and `ProfileDisplay` — all replaced by the unified set. Added `PlusSignIcon`/`MinusSignIcon` to the Follow button (plus = follow, minus = unfollow), fixed all three icons to forward `className` to the SVG. Added self-service page membership via a new `/api/pages/[pageId]/membership` route (GET/POST/DELETE) and `JoinButton` component — visible only when acting as personal identity, shows Join / Leave group / disabled Member depending on the viewer's role (ADMIN/EDITOR are members by default and cannot self-leave). Also extended `TransparentCTAButton` to support a button variant (`onClick` + `disabled`) alongside the existing link variant, and added `MESSAGE_PAGE_CONVERSATION` route constant + wired `?type=page` query param support into the conversation route so messaging pages works end-to-end. Follow and Join/Leave buttons use matching icon sets (`PlusSignIcon`/`MinusSignIcon` and `UserPlusSignIcon`/`UserMinusSignIcon` respectively), and the button column is fixed at `w-36` so the layout never shifts between label states.

#### Entry: Thu 04/02/2026 12:08 PDT
Fixed page-based messaging bug where messages sent "as a Page" were actually sent as the personal user. The root cause was `/messages/[userId]/page.tsx` — the direct conversation route reached via "Send Message" on a profile — which never read `activePageId` from `ActiveProfileContext`, so `asPageId` was never passed to `ConversationThread`. This meant the API created conversations under the personal user identity instead of the page, causing threads to not appear in the page's inbox and messages to display as from the user instead of the page. Also updated `isSent` in `ConversationThread` to recognize messages sent as the active page (multi-admin support) and `isUnread` in `MessagesPageView` to not flag page-sent messages as unread for fellow page admins. Fixed a conversation data leak in the GET/PATCH `/api/messages/conversation/[targetId]` route where `getAllConversationIds` returned conversations from all managed pages — so a page admin viewing their personal thread with a page could see that page's private conversations with other users; replaced with identity-scoped `getConversationIdsForIdentity(userId, asPageId)` and wired `asPageId` through from the UI as a query param (GET) and request body (PATCH).

#### Entry: Wed 04/01/2026 17:33 PDT
Refactored unread message notifications to be profile-scoped. The unread-count API now returns `{ personal, pages: { [pageId]: count } }` instead of a flat total. A new `UnreadCountContext` centralizes polling (60s, visibility-gated), re-fetches on profile switch, listens for a `messages:read` custom event dispatched after mark-as-read, and exposes `activeCount` for the current profile. `HamburgerMenu` lost its local polling effect and now reads from context. `NavProfileTag` shows a `NotificationDot` next to inactive profiles in the switcher when they have unread messages. Added 7 new e2e tests: 3 in `messaging.spec.ts` (inbox list, open thread, notification dot clear) and a new `profile-switching.spec.ts` with 4 tests covering switcher visibility, page/personal identity transitions, and profile-scoped inbox filtering. The dot test uses a second browser context to send a fresh message, making it independent of seeded read state.

#### Entry: Sun 03/29/2026 15:25 PDT
The messages flow was overhauled by replacing the old multi-page navigation with a TabbedPanel-based MessagesPageView, where thread tabs open inline and the left panel reflects only the active profile — switching profiles clears tabs and reloads the inbox. A core identity bug was fixed so that messages sent as a page (e.g. PMG) correctly create [page:PMG, user:Sam] conversations rather than personal user threads, and the GET conversation endpoint was updated to find threads via managed pages consistently with the existing PATCH behavior. Two display bugs were resolved: isSent now reads from useActiveProfile() instead of incorrectly comparing against targetId, and scroll behavior was fixed by using scrollTop = scrollHeight on the overflow container instead of scrollIntoView(), which had been hiding the tab bar. Finally, unread message notifications were added via a NotificationDot component overlaid on the hamburger icon and Messages menu row, backed by 60-second polling; opening a thread marks messages read via PATCH and optimistically reverts the bold unread styling.

#### Entry: Fri 03/27/2026 18:01 PDT
Added basic observability and unit tests for profile switching. Added `@vercel/analytics` to root layout, structured JSON request logging in `proxy.ts`, and a `logAction()` helper wired into 7 write paths (login, signup, post/event/page/follow/message created). Logs queryable via Vercel dashboard or CLI with `jq`. Documented in `docs/scratch/logging_and_tracking.md`. Filled `permission.test.ts` placeholder (`canPostAsPage`, `canManagePage`). Added `active-page-route.test.ts` (PUT/DELETE auth + permission gate). Added `ActiveProfileContext.test.tsx` (identity resolution, switchProfile success/403/null, fetchPages filtering). 88 tests passing, typecheck clean.

#### Entry: Fri 03/27/2026 17:41 PDT
Completed profile switching (ActiveProfileContext) and event authorship features. Replaced `SessionContext` with `ActiveProfileContext` — a full provider supplying `activeEntity`, `activePageId`, `currentUser`, `pages`, `switchProfile`, and `fetchPages` app-wide. All profile switching now routes through `PUT/DELETE /api/session/active-page` (validates ADMIN/EDITOR permission server-side), closing a security gap in the old `PUT /api/me/page` route which ignored `activePageId`. Added `NavProfileTag` dropdown with "View Profile" and "Switch Profile" menu items (icons, accordion-style inline expansion). Added `variant="compact"` to `ProfileTag` (no handle, badge stacked below name) and `align` prop (`"left"` default fixes button-ancestor centering, `"right"` for ConnectionsView left tabs). Nav trigger uses compact+badge ("Me" for personal, role for pages). Event authorship implemented on the draft event page (`EventPageClient`) — clicking the organizer ProfileTag opens a selector showing all switchable identities (personal + ADMIN/EDITOR pages); saves immediately via `PATCH /api/events/[id]`; read-only once published. Active identity excluded from the selector list. Hover behavior matches the nav profile tag trigger.

#### Entry: Fri 03/27/2026 12:53 PDT
Added Playwright E2E test suite (19 tests, all passing). Installed `@playwright/test`, created `playwright.config.ts` (chromium, serial workers, localhost:3000), and a shared `tests/helpers/auth.ts` login utility. Test files cover: public page renders (`public.spec.ts`), auth flows including login/logout/signup/protected-route redirect (`auth.spec.ts`), content authoring — event inline-edit+publish, post creation, page creation (`authoring.spec.ts`), messaging between users (`messaging.spec.ts`), and profile pages with follow/unfollow toggle (`profile.spec.ts`). Run with `npm run test:e2e`. Tests run serially since they share the local dev DB; seed must be run first. Noted that the signup test handles the in-memory rate limiter (5 signups/hr per IP) gracefully.

#### Entry: Thu 03/26/2026 23:20 PDT
Created `ProfileTag` — unified entity row (avatar + name + handle + badge + actions) accepting `CardEntity` (`CardUser | CardPage`) and resolving type internally. Refactored `ProfilePicture` to the same single `entity` prop. Replaced all one-off avatar/profile patterns across the app: `ConnectionsPageView`, `ConnectionsView`, both settings pages, `EventPageClient`, `ConnectionListItem`, `PageSwitcher`. Deleted `UserCard` and `PageCard`. Added `NavProfileTag` in the nav bar as a `DropdownMenu` trigger; removed Profile from the hamburger menu.

#### Entry: Thu 03/26/2026 15:51 PDT
Production deployment and bug fixes. Applied 3 pending migrations to prod and reseeded after data drop. Fixed posts missing from user and page profiles — a prior refactor left `// Collection items are just events now` in both profile pages, silently dropping posts; added `getPostsByUser`/`getPostsByPage` utilities and wired them in. Fixed hydration mismatch in `FilteredCollection` — `useBreakpoint` called getter during `useState` init so client and server diverged on column count; fixed with explicit `initialValue` param (also applied to `useDeviceType`). Fixed `getUserMemberships` hardcoding `role: MEMBER` — now returns all roles so connections view "Me" tab shows full page membership.

#### Entry: Mon 03/23/2026 19:34 PDT
Fixed collection page performance and added post detail page. Eliminated N+1 fetch problem on explore page — each `CollectionCard` was embedding `PostsList` which fired individual API requests per card, causing cascading flashing renders. Replaced with eagerly-loaded `_count` and `recentUpdate` from the initial API response using Prisma `_count` and `take: 1` eager loading. Created shared collection field selectors (`eventCollectionFields`, `postCollectionFields`) in `fields.ts` that extend base selectors. Switched collection grid from CSS `columns` to modulo-distributed flex columns with new `useColumnCount` hook for responsive 1/2/3 column layout. Created post detail page at `src/app/posts/[id]/page.tsx` (posts were 404ing — no route existed after old project detail page was removed in v0.4 migration). Added `DeletePostButton` following `DeleteEventButton` pattern. Collection cards now show inline recent update preview with left-border accent and "N updates" link to detail page. Standardized all "reply/replies" labels to "update/updates". Fixed profile/explore parity — updated `getEventsByUser` and `getEventsByPage` to use `eventCollectionFields` so profile pages show the same update data as explore.

#### Entry: Sun 03/22/2026 22:09 PDT
Addressed all 17 PR #2 review comments. Consolidated three duplicate initials implementations into canonical `getUserInitials`/`getPageInitials` in `utils/text.ts`. Deleted unused files: `OwnerContext.tsx`, `ProfilePicPlaceholder.tsx`, `UserCollectionSection.tsx`. Renamed `UserCollectionSection` → `ProfileCollectionSection` (shared by user and page profiles). Refactored `EntityAvatar` with clean `resolveEntity()` extractor. Added `validatePostData()`/`validatePostUpdateData()` to `validations.ts`. Updated `ConnectionsView` to use `EntityAvatar`. Updated `PROJECT_GUIDELINES.md` with v0.4 schema diagram, expanded tech stack, key conventions, and UI component map.

#### Entry: Sat 03/21/2026 15:32 PDT
Completed schema v0.4 migration — full codebase propagation. Eliminated Owner, Org, and Project models entirely. Pages replace both Orgs and Projects as a unified shared-resource concept. Permission table (ADMIN/EDITOR/MEMBER roles on PAGE/EVENT resources) replaces OrgMember roles and Owner-based access. All content (Posts, Events, Images) now references `userId` directly with optional `pageId` instead of `ownerId`. Follow model split into `followingUserId`/`followingPageId` (polymorphic nullable FKs). Messaging rewritten from direct sender/receiver to conversation-based model with polymorphic ConversationParticipants (User or Page), enabling shared Page inboxes for ADMIN/EDITOR users. Posts support one-level-deep update threads via `parentPostId`.

Propagated changes across entire codebase: deleted ~20 obsolete API routes (owners/*, orgs/*, projects/*), created ~10 new Page API routes (pages/*, pages/[id]/members/*, me/page, me/pages, session/active-page). Rewrote follows, posts, messages, and image API routes for new schema. Replaced all owner/org/project components with page-based equivalents (EntityAvatar, ProfileDisplay, ProfileHeader, PageCard, PageBanner, PageSwitcher, etc.). Updated all app pages — deleted /o/*, /projects/*, /orgs/new; created /p/[slug], /p/profile/*, /pages/new; fixed /u/*, /events/*, /explore, /collections, /messages. Updated seed script, auth flow, session context, and validation helpers. Final result: 0 TypeScript errors (down from 376), Prisma schema validates clean.

#### Entry: Tue 02/03/2026 20:09 PST
Fixed profile/session owner issues for user and org switching flows. Resolved infinite redirect loop at `/o/profile` when not acting as org (was redirecting to itself). Created server utilities `canEditUserProfile()` and `canEditOrgProfile()` in `session.ts` for verifying session owner permissions. When acting as org and visiting `/u/profile`, now redirects to `/o/profile` with dismissible tooltip banner ("You are logged in as @org-slug, switch to user to edit"). Created `ActingAsOrgTooltip` component for this banner. Updated profile Options menus: on user public profile (`/u/[username]`), `ProfileOptionsMenu` now self-checks session for org state and shows only "Switch to User Profile" when acting as org. Created `OrgProfileOptionsMenu` for org public profiles (`/o/[slug]`) - shows full options (Edit, New Project, New Event, Settings) when acting as that org, or "Switch to Org Profile" when admin but not currently acting as org. Cleaned up `OwnerProfileHeader` to be org-specific with accurate documentation, removing dead code for USER type handling. Refactored to eliminate prop drilling - `ProfileOptionsMenu` now uses `useSession()` directly instead of receiving `isActingAsOrg` prop from parent chain.

#### Entry: Sat 01/31/2026 22:34 PST
Refactored user profile settings page (`/u/profile`) with cohesive settings layout. Created "User Settings" section with View Public Profile, Edit Profile (triggers inline editing), and disabled/mocked buttons for upcoming features (Privacy Settings, Change Password, Delete Account, Manage Followers, Manage Orgs) with "Coming Soon" badges. Added "Org Settings" section with Create Organization button and org list showing View/Switch To buttons for each org. Simplified `EditableProfile` component to support controlled editing state via props, enabling the Edit Profile button to trigger inline editing and scroll to the Profile Information section. Created shared profile settings component library in `src/lib/components/profile-settings/` with `ProfileSettingsBase`, `SettingsSection`, `DisabledSettingsButton`, `LineDivider`, and `OrgSwitcher` components. Refactored both `/u/profile` and `/o/profile` pages to use shared components, ensuring consistent design and functionality between user and org profile settings. The `OrgSwitcher` component includes "Switch to User Profile" option when viewing org profiles.

#### Entry: Fri 01/30/2026 19:53 PST
Extracted view toggle buttons into reusable component. Created `ViewToggle.tsx` in `src/lib/components/filter-sort/` containing Grid, List, and Map view buttons previously inline in `FilterBoard`. Added `ViewToggleButton` component following the same pattern as `CollectionTypeButton` from `CollectionTypeBadge.tsx` - uses consistent styling with `bg-moss-green`/`bg-misty-forest` colors, uppercase text, bold font, and hover shadow effects. Updated `FilterBoard` to import and use the new `ViewToggle` component, improving code organization and reusability.

#### Entry: Wed 01/28/2026 20:58 PST
Reset production Supabase database for v0.3 schema deployment. Dropped and recreated public schema via `prisma db execute` to resolve migration conflict where `OrgRole` type already existed from partial prior attempt. Successfully applied both migrations (`v03_schema_init` and `make_circular_fks_deferrable`) and ran seed script. Production database now has fresh schema with seed data (users, orgs, projects, events, posts, messages, follows).

#### Entry: Wed 01/28/2026 19:20 PST
Fixed database seeding for v0.3 schema migration. Created migration `20260129030218_make_circular_fks_deferrable` to make User ↔ Owner circular FK constraints `DEFERRABLE INITIALLY DEFERRED`, enabling atomic creation of both records in a single transaction. Updated `seed.ts` and `createUserWithOwner()` in `user.ts` to work with deferred constraints. Made Supabase optional for local development - seed now uses local static files from `/public/static/examples/` when `NEXT_PUBLIC_SUPABASE_URL` isn't set, falling back to `/static/examples/filename.png` URLs that Next.js serves directly. Cleaned up verbose debug logging from seed file. Ready to run `npx prisma generate && npm run db:seed:dev` to complete seeding.

#### Entry: Wed 01/28/2026 18:09 PST
Eliminated type casting in server utilities by deriving types from Prisma schema. Added `OwnerFromQuery`, `ProjectFromQuery`, and `EventFromQuery` types to `fields.ts` using Prisma's `GetPayload` utility, making the schema the source of truth. Created `toProjectItem()` and `toEventItem()` transform functions that convert Prisma results to client types without `as unknown as` casts. Updated `PublicOwner` type to use `OwnerType` enum from Prisma instead of literal strings. Fixed outdated `PublicUser` type by replacing `actorId` with `ownerId` to match current schema. Added `isUserOwner()` and `isOrgOwner()` type guard helpers and `PublicOrg` type export. Removed backward compatibility for deprecated `dateTime` field in events API (now strictly `eventDateTime`). Refactored `getOwnerId()` to return `owner.id` directly, added `getOwnerHandle()` and `getOwnerEntityId()` for specific lookups. 70 TypeScript errors remain in seed file (29) and frontend components (41) pending refactor.

#### Entry: Mon 01/12/2026 21:27 PST
Implemented org creation feature. Added "Create Organization" button to user profile page and org creation form at `/orgs/new` with name, slug (auto-generated), headline, bio, interests, and location fields. Built `POST /api/orgs` endpoint with `validateOrgData()` validation. Added `createOrg()` server utility that creates Actor (type ORG), Org record, and OrgMember with OWNER role. Created slug generation utility for URL-safe slugs. Form follows existing profile creation patterns. Features marked "coming soon" (parent topic, admins, private/public) deferred pending schema changes.

#### Entry: Sun 01/11/2026 12:07 PST
Refactored routing architecture for unified actor-based design. Created unified profile routes `/u/profile` and `/o/profile` with matching edit and settings pages, replacing scattered profile routes. Implemented `ActorContext` to manage current actor state (user or org) throughout the app. Added org switching functionality via `/api/auth/switch-org` and `/api/auth/switch-to-user` routes. Consolidated API routes to `/api/me/*` pattern with unified `/api/me/actor` endpoint returning current actor based on session, replacing separate profile endpoints. Updated navigation bar to route to org profile when acting as org. Removed obsolete route constants and cleaned up unused profile pages. All profile pages now use consistent actor-based patterns with unified edit and settings functionality.

#### Entry: Fri 01/09/2026 21:45 PST
Implemented org profile functionality with "Switch to [Org] Profile" button on user profile page. Created public org profiles at `/o/[slug]` and private settings at `/profile/org/[slug]`, both using Actor-based components (`ActorProfileHeader`, `ActorProfileDisplay`) that work for both Users and Orgs. Added org server utilities and types. Enhanced project/event utilities with `getProjectsByActor()` and `getEventsByActor()` for unified actor handling. Fixed placeholder Supabase URLs in database via `fix-image-urls.ts` script. Added `displayName` field to User model with priority: `displayName` > `firstName + lastName` > `username`. Updated all display name utilities to use unified priority system.

#### Entry: Fri 01/09/2026 20:17 PST
Completed major schema migration from v1 to v2. Migrated Prisma schema to v2 introducing Actor abstraction layer (unified identity for Users and Orgs), replaced Entry model with Post model (standalone or descendant posts), and implemented polymorphic ImageAttachment system replacing direct foreign keys. Updated all TypeScript types, server utilities, API routes, and components to use new schema patterns (ownerActorId instead of ownerId, Actor relations, ImageAttachment queries). Split User.name into firstName/middleName/lastName fields. Added avatar support for Users and Orgs. Created comprehensive environment file system using Next.js conventions: `.env` (base config with AUTH_SECRET), `.env.development` (local database), `.env.production` (remote Supabase database). Updated all scripts to respect environment files: `npm run dev` uses local DB, `npm run dev:remote` uses production DB, `npm run db:seed:dev` seeds local, `npm run db:seed` seeds production. Created `db:migrate:deploy` script for production migrations. Updated seed script and `prisma.config.ts` to load environment files in correct order. Created deployment documentation (`docs/DEPLOYMENT.md`) explaining migration workflow. All code updated, migrations created, and local testing successful. Ready for production deployment.

#### Entry: Wed 12/31/2025 23:15 PST
Fixed client/server boundary issues and build errors. Refactored `AboutModal` to accept `username` as a prop instead of fetching internally. Created `user-client.ts` utility following existing patterns (`project-client.ts`, `event-client.ts`) with `fetchProfile()` function for client-side profile fetching. Updated `NavigationIcons` to properly fetch username using `fetchProfile()` utility with `useEffect` hook, fixing incorrect `await` usage in non-async component. Removed server-only imports from `auth-client.ts` (`getUserById`, `auth`) that were causing Prisma to be bundled into client bundle, breaking the build. Updated `layout.tsx` and `NavigationBar` to pass `session` prop through component tree instead of `userHomeLink`.

#### Entry: Tue 12/30/2025 21:43 PST
Extended edit and delete functionality to projects, matching events pattern. Added PUT/DELETE handlers to `/api/projects/[id]` with ownership verification. Created reusable `EditProjectForm` component for both create and edit modes, refactored `/projects/new` to use it. Added `DeleteProjectButton` with confirmation dialog. Implemented image cleanup: `deleteEvent()` and `deleteProject()` now remove associated images from Supabase storage bucket before deletion, preventing orphaned files. Database records cleaned up via Prisma cascade. Improved error handling to return specific error messages. Added `ProjectUpdateInput` type and validation. All project and event CRUD operations now follow consistent patterns.

#### Entry: Tue 12/30/2025 14:49 PST
Refactored image storage and entry system to use extensible polymorphic schema. Created new `Image` model in Prisma schema supporting multiple images per collection item (Project, Event) with alt text and upload tracking. Replaced single `imageUrl` field on Project and `imageUrls` array on Event with proper `Image` relations. Created new `Entry` model for collection updates/entries using polymorphic pattern (`collectionType` + `collectionId`) instead of explicit foreign keys, making it extensible for future collection types (Post, etc.). Removed obsolete `ProjectEntry`, `ProjectEntriesList`, and `ProjectEntryUpdate` components that no longer fit the schema. Refactored `ProjectCard` to inline project description, owner info, and image display logic. Created generic `entry-client.ts` with `getEntries(collectionId, collectionType)` function that works with any collection type. Updated seed file to upload local images from `public/static/examples` to Supabase `uploads/examples` bucket and create corresponding Image records. Fixed client/server boundary issues where client components were importing server-only Prisma code. Updated all TypeScript types to match new schema, removed deprecated `ProjectEntryItem` type. Created reusable Prisma field selection objects in `fields.ts` for consistent data fetching. Schema now supports multiple images per item and extensible entry system for all collection types.

#### Entry: Sat 12/27/2025 19:56 PST
Reorganized codebase to prevent client/server boundary issues. Moved all server-only files (prisma, project, event, user, message, project-entry) to `src/lib/utils/server/` folder with warning comments. Updated all imports across codebase to use new server paths. Fixed build error where client components were importing server code (ProjectEntriesList was importing from server file). Added proper type validation to server functions - `getProjectEntries()` and `createProjectEntry()` now explicitly return `ProjectEntryItem` types with proper field selection. Extracted navigation icons into reusable components (`src/lib/components/icons.tsx`). Created `LoginLogoutIcon` component to handle authentication UI. Added "New Entry" button to project detail page (visible only to project owners) with new entry creation page at `/projects/[id]/entries/new`. Created `CLIENT_SERVER_BOUNDARIES.md` guide documenting best practices for avoiding client/server import issues.

#### Entry: Sat 12/27/2025 18:33 PST
Fixed first production bug: email case sensitivity causing login failures. Prisma/PostgreSQL email lookups are case-sensitive, causing `Diana.example@example.com` to fail against seed data with lowercase `diana.example@example.com`. Normalized emails to lowercase in `authorize()` function (`src/lib/auth.ts`) and signup route before database operations. Added logout button to NavigationBar.

#### Entry: Sat 12/27/2025 16:53 PST
Resolved Vercel/Supabase deployment issues. Fixed Prisma build error by removing duplicate `generator client` block in `schema.prisma` (had two conflicting generator definitions). Fixed NextAuth session 500 errors by adding `secret` configuration to NextAuth config (`AUTH_SECRET` or `NEXTAUTH_SECRET` env var required for production). Added comprehensive error handling to auth flow: wrapped `authorize` function and `session` callback in try-catch blocks to prevent unhandled errors from causing 500 responses. Added `jwt` callback to properly store user ID in token for session retrieval. Improved Prisma client initialization in `src/lib/utils/prisma.ts` with better error messages and connection pool settings for serverless environments. Deployment now requires `AUTH_SECRET` environment variable to be set in Vercel dashboard. MVP is COMPLETE!

#### Entry: Thu 12/25/2025 22:31 PST
Extracted filtering and sorting logic into reusable components. Created `useFilter` hook to encapsulate filter/sort/view state management. Built `FilterBoard` component for UI controls (search, filter tabs, sort dropdown, view toggles). Reorganized `CollectionPage` as presentational component accepting all state/handlers as props. Refactored `collections/page.tsx` to client component handling data fetching and state, passing props to `CollectionPage`. Added personal collection section to profile page (`/profile`) displaying user's projects and events with full filtering/sorting capabilities. Made `CollectionPage` reusable with optional `title` prop for context-specific headers. Architecture now cleanly separates data fetching (client/server), state management (hooks), and presentation (components).

#### Entry: Thu 12/25/2025 21:54 PST
Refactored collections rendering architecture. Created `FilteredCollection` component to handle view state (map/list/grid) and render appropriate card types directly (ProjectCard/EventCard), making `CollectionCard` obsolete. Extracted `CollectionItemCard` helper to DRY up card rendering logic. Added events to seed data (two events from admin and alice users). Renamed type interfaces: `Event` → `EventItem` and `Project` → `ProjectItem` to avoid conflicts with DOM Event type, updated all references across codebase.

#### Entry: Thu 12/25/2025 17:47 PST
Completed Milestone F code review and implemented type discriminator pattern. Added `type` field to Prisma schema (Project/Event models) with defaults, making database the source of truth. Refactored type guards to use discriminator field. Created shared collection utilities (`src/lib/utils/collection.ts`) eliminating ~60 lines of duplicated filtering/sorting logic between collections pages. Updated migrations to include `type` field properly. All utility functions now return data directly from Prisma without manual type mapping.

#### Entry: Thu 12/25/2025 14:46 PST
Normalized project data fetching and aligned type system with Prisma schema. Refactored project utilities to remove transformation layer - types now match Prisma directly (Date fields instead of string, no manual conversion). Created `PublicUser` type matching `publicUserFields` selection and updated `Project` interface to use `PublicUser` for owner instead of limited `ProjectOwner`. Updated `getInitials()` to accept `PublicUser` object. Renamed `publicProfileFields` to `publicUserFields` throughout codebase for consistency. Refactored component structure: moved project entry content to `ProjectEntry` component, updated `ProfilePicPlaceholder` to accept full project and made circle clickable link to user profile. Added client-side fetch utilities (`fetchProjects`, `fetchProjectById`) in `project.ts` for consistent API usage. All server-side functions now return Prisma results directly without transformation, ensuring type safety and eliminating unnecessary data conversion overhead.

#### Entry: Tue 12/23/2025 14:13 PST
Completed messaging feature implementation (Milestone E - messaging components). Added Message model to Prisma schema with sender/receiver relations and User model updates. Created message utilities (`src/lib/utils/message.ts`) with `getConversations()`, `getMessages()`, and `sendMessage()` functions. Added message validation (`validateMessageContent()`) and TypeScript types (`src/lib/types/message.ts`). Built API routes: GET/POST `/api/messages` for conversations list and sending messages, GET `/api/messages/[userId]` for individual conversation threads. Created frontend pages: `/messages` (conversations list with last message preview) and `/messages/[userId]` (individual conversation with message input). Added "Messages" link to navigation sidebar (visible when logged in). Integrated message buttons on user profile pages (`/u/[username]`) and project detail pages (`/projects/[id]`) to start conversations. Updated `getUserByUsername()` to include user ID for messaging functionality. Updated README with WSL/Ubuntu PostgreSQL setup instructions. Database migration pending - need to configure PostgreSQL authentication (setting postgres user password). All messaging endpoints are protected and include validation (prevents self-messaging, validates content length).

#### Entry: Thu 12/18/25 22:42 PST
Implemented image upload feature for projects (Milestone E - image components). Added `imageUrl` field to Project model in Prisma schema, created migration. Built image upload API route (`/api/projects/upload`) with file validation (type, size limits). Updated project creation form to support image upload with preview. Enhanced project list and detail pages to display images using Next.js Image component. Fixed ESLint configuration issues by creating custom `eslint.config.mjs` with TypeScript, React, and React Hooks support (workaround for Next.js 16.0.10 `next lint` bug). Migrated from `middleware.ts` to `proxy.ts` per Next.js 16 deprecation. Fixed image display issues by switching from `fill` prop to explicit dimensions. Reorganized code structure (moved utilities to `src/lib/utils/`). Added uploads directory to `.gitignore`. Images now display correctly on project listings and detail pages.

#### Entry: Thu 12/18/25 17:56 PST
Completed Milestone C MVP (Projects). Created plan document (`ai-tools/MILESTONE_C_PLAN.md`). Added Project model to Prisma schema with User relation, ran migration. Created `src/lib/project.ts` with utility functions and extended validations for projects. API routes: GET/POST `/api/projects` (public listing with search, protected create) and GET `/api/projects/[id]`. Frontend: Created `/projects` (public listing), `/projects/new` (protected form), and `/projects/[id]` (public detail) pages. Updated navigation with Projects and New Project links. Standardized auth handling across client and server components.

#### Entry: Thu 12/18/25 16:59 PST
Completed pre-Milestone C refactoring and code review. Created `src/lib/errors.ts` with standardized error response helpers (unauthorized, notFound, badRequest, serverError). Created `src/lib/validations.ts` with reusable validation functions for email, username, password, and profile data. Extended `src/lib/user.ts` with `updateUserProfile()` function to extract profile update logic from API routes. Updated `/api/profile` and `/api/auth/signup` routes to use new utility functions for consistency. Documented that DELETE operation is intentionally omitted from MVP. Created comprehensive milestone review document (`ai-tools/MILESTONE_REVIEW.md`) confirming Milestone A and B completion. All refactors improve code maintainability, testability, and consistency. Ready to proceed with Milestone C (Projects).

#### Entry: Wed 12/17/25 17:34 PST
Completed Milestone B (User Profile). Created `/profile`, `/profile/edit`, and `/u/[username]` pages with API routes. Added `src/lib/user.ts` utility for reusable profile queries. Fixed session user.id not being passed through by adding NextAuth callback. Added SessionProvider to layout for client-side session access. Added profile link to home page and logged-in banner to login page.

#### Entry: Wed 12/17/25 17:16 PST
Completed Milestone A (Authentication). Fixed Prisma v7 compatibility by adding `@prisma/adapter-pg` driver adapter. Added protected route middleware using session cookie check (Edge runtime compatible). Created bare-bones `/profile` page for testing auth redirect. Fixed Tailwind v4 PostCSS config and module format issues.

#### Entry: Wed 12/17/25 16:54 PST
Implemented authentication (Milestone A). Added NextAuth with credentials provider, bcrypt password hashing, signup API route, and login/signup pages. Created Prisma client singleton in `src/lib/`. Auth flow: signup creates user with hashed password, login validates credentials against database.

#### Entry: Wed 12/17/25 16:33 PST
Connected local PostgreSQL to Prisma. Fixed Prisma v7 config issue (URL moved from `schema.prisma` to `prisma.config.ts`). Ran first migration successfully—`users` table now exists in local database. Updated `.env` with correct connection string format for local dev.

#### Entry: Wed 12/17/25 16:03 PST
Added Prisma ORM with PostgreSQL. Created initial User model in `prisma/schema.prisma` covering auth fields (email, passwordHash, username) and profile fields (name, headline, bio, interests, location). Added `database/README.md` with local PostgreSQL setup instructions. Next: configure local PostgreSQL and run initial migration.

#### Entry: Wed 12/17/25 15:54 PST
Initialized Next.js project manually (App Router, TypeScript, Tailwind CSS). Created core config files: `tsconfig.json`, `next.config.ts`, `tailwind.config.ts`, `postcss.config.mjs`. Set up base `src/app/` structure with layout, page, and global styles. Added `.gitignore` for node_modules and build artifacts. Project is now runnable with `npm run dev`.

#### Entry: Wed 12/17/25 15:38 PST
I initialized the project plan and a folder with tools for the AI Agent such as the project guidelines with best practices, and the journal for notes about sessions and what has been done. No git or code yet.

