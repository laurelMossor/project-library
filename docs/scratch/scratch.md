Last two journal entries:
#### Entry: Sun 03/29/2026 15:25 PDT
The messages flow was overhauled by replacing the old multi-page navigation with a TabbedPanel-based MessagesPageView, where thread tabs open inline and the left panel reflects only the active profile — switching profiles clears tabs and reloads the inbox. A core identity bug was fixed so that messages sent as a page (e.g. PMG) correctly create [page:PMG, user:Sam] conversations rather than personal user threads, and the GET conversation endpoint was updated to find threads via managed pages consistently with the existing PATCH behavior. Two display bugs were resolved: isSent now reads from useActiveProfile() instead of incorrectly comparing against targetId, and scroll behavior was fixed by using scrollTop = scrollHeight on the overflow container instead of scrollIntoView(), which had been hiding the tab bar. Finally, unread message notifications were added via a NotificationDot component overlaid on the hamburger icon and Messages menu row, backed by 60-second polling; opening a thread marks messages read via PATCH and optimistically reverts the bold unread styling.

#### Entry: Fri 03/27/2026 18:01 PDT
Added basic observability and unit tests for profile switching. Added `@vercel/analytics` to root layout, structured JSON request logging in `proxy.ts`, and a `logAction()` helper wired into 7 write paths (login, signup, post/event/page/follow/message created). Logs queryable via Vercel dashboard or CLI with `jq`. Documented in `docs/scratch/logging_and_tracking.md`. Filled `permission.test.ts` placeholder (`canPostAsPage`, `canManagePage`). Added `active-page-route.test.ts` (PUT/DELETE auth + permission gate). Added `ActiveProfileContext.test.tsx` (identity resolution, switchProfile success/403/null, fetchPages filtering). 88 tests passing, typecheck clean.

TASK:
Next, we're going to ensure that the notification icon only renders on the hamburgermenu when the user is on the relevant active profile. For example:
Alice is logged in and her default state is for the active profile be their personal profile. If they are admin/editor of any Pages then their profile switching button will active profile to a Page. Alice does not have any unread messages right now and there is no notifications icon present. Her Page, Portland Makers Guild has a new message. She switches to her page via the dropdown, and now that her active profile is PMG, the hamburger menu will have the notification icon overlaid, and when opening the dropdown, the notification icon will be next to the messages option. That part exists, just calling out that it appears in two ways currently. Once she navigates to the messages page and opens the message, the only unread message, the notifications icon will disappear. 

Investigate what needs to change for this to work as I've described. Additionally, please review the unread count api implementation and it's usage. I am having thoughts about it, but it's hard to articulate why I think it is a bit weird. Take a critical eye from the perspective of a senior software engineer to make your suggestions. 