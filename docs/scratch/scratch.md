We're going to be working on the Messages page. Right now, Messages are only build for users and not Pages. Take a look at the ConnectionsViewPage, you can see this TabbedPanel and how it creates a matrix view to see follows/membership across profiles. We will take that similar pattern (same component and styling) in order to build out the messages interface. This time, use the same data source that the NavBarProfileTag uses to renders the messages for the active profile, with left tab being the active profile (you can still change the active profile from the navbarprofiletag). Take the liberty to consider common sense refactors to that tabbed panel so that it can be extended for use on these two components seamlessly. The top tab(s) will just read "Direct Messages", one tab. When clicking on a message thread, it will span a second tab, loads the thread, then switches the view to that tab. When leaving that tab, remove the tab. Design consideration: currently, any message thread exists at it's out route, how good of behavior/performance can we get by having that second tab be an iframe with the message thread page inside that? That way, a user can manage messages from one location by switching between profiles. 

Next, consider what a notifications architecture would look like. Right now, messages are handled by polling. Do what you need to do to expose that data to the HamburgerMenu. I want to use the ThinkLineDotIcon in novel read over the hamburgermenu icon to signify a new message, and then on the dropdown menu at the end of the row to have the same dot icon. 